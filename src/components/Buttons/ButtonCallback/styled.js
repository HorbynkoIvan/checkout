import styled from 'styled-components/macro';

export const ScButtonCallback = styled.button`
  cursor: pointer;
  border: 0;
  font: 400 14px / 21px ${({ theme }) => theme.mainFont};
  text-decoration-line: underline;
  background: none;
  outline: none;
  color: #000000;
`;
