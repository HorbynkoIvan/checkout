import React from 'react';
import { ButtonCallback } from './ButtonCallback';

export default {
  title: 'Buttons/ButtonCallback',
  component: ButtonCallback,
};

const Template = (args) => <ButtonCallback {...args} />;

export const Primary = Template.bind({});
