import React from 'react';

import { PhoneDropdown } from './PhoneDropdown';

export default {
  title: 'Containers/PhoneDropdown',
  component: PhoneDropdown,
};

const Template = (args) => <PhoneDropdown {...args} />;

export const Primary = Template.bind({});
