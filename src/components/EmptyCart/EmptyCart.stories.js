import React from 'react';
import { EmptyCart } from './EmptyCart';

export default {
  title: 'Basic/EmptyCart',
  component: <EmptyCart />,
};

const Template = () => <EmptyCart />;

export const Primary = Template.bind({});
