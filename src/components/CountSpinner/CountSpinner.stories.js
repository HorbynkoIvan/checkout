import React from 'react';
import CountSpinner from './index';

export default {
  title: 'Basic/CountSpinner',
  component: CountSpinner,
};

const Template = (args) => <CountSpinner {...args} />;

export const Primary = Template.bind({});
