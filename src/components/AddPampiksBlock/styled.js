import styled from 'styled-components/macro';

export const ScAddPampiksBlock = styled.span`
  font: 500 12px / 16px ${({ theme }) => theme.mainFont};
  color: #693bff;
  display: inline-block;
  white-space: nowrap;
  text-align: right;
`;
